package com.decoded.cauldron.api.config;

/**
 * Scopes for Scoped Configuration.
 */
public class Scopes {
  /**
   * Root scope (has no path).
   */
  public static final ConfigScope ROOT = new ConfigScope();
}
